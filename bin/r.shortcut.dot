#. s.ssmuse.dot "$@"
#return                 # compatibility mode, new code not active yet

#ENV_TEST_MODE='-alpha'
[[ -z $CMD_sed ]] && eval $(afterburn_commands --export sed xargs cat grep rm)
[[ -f $TMPDIR/bin/true_path ]] || ln -s `which true_path` $TMPDIR/bin/true_path
[[ -f $TMPDIR/bin/r.shortcut-cfg ]] || ln -s `which r.shortcut-cfg` $TMPDIR/bin/r.shortcut-cfg
. r.shortcut-2.dot 2>/dev/null
